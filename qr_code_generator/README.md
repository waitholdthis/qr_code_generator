# QR Code Generator

This repo lives under `qr_code_generator/` and supports turning images into QR codes in two ways:

- **Data mode**: `qr_code_generator -m data -i <image> -o out.png`
- **Brand mode**: `qr_code_generator -m brand -i <input> -o out.png`

See `README.md` for usage details.